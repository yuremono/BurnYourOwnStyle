---
interface Props {
  class?: string
  style?: Record<string, string>
}

const { class: className = '', style } = Astro.props
---

<div class={`FlexR ${className}`} style={style}>
  <slot />
</div>
